import { NextResponse } from "next/server";
import { buildWalletEstimate, fetchCollectionFloorPriceETH, fetchOpenSeaJson, isEns, isEthAddress } from "../shared";
export async function GET(req: Request) { const { searchParams } = new URL(req.url); const wallet = String(searchParams.get("wallet") || "").trim(); const slug = String(searchParams.get("slug") || "").trim().toLowerCase(); if (!wallet || (!isEthAddress(wallet) && !isEns(wallet)) || !slug) return NextResponse.json({ targetCollection: null, count: 0, estimateQuality: "low", error: "invalid_input" }); const estimate = await buildWalletEstimate(wallet); if (estimate.error) return NextResponse.json({ targetCollection: null, count: 0, estimateQuality: "low", error: "estimate_failed" }); const metadata = await fetchOpenSeaJson<{ name?: string; image_url?: string; imageUrl?: string }>(`/collections/${encodeURIComponent(slug)}`, {}); const floorPriceETH = await fetchCollectionFloorPriceETH(slug); if (!floorPriceETH || floorPriceETH <= 0) return NextResponse.json({ targetCollection: null, count: 0, estimateQuality: estimate.estimateQuality, error: "no_floor" }); const count = Math.floor(estimate.estimatedValueETH / floorPriceETH); const error = count === 0 ? "zero_result" : estimate.estimateQuality === "low" ? "low_quality" : null; return NextResponse.json({ targetCollection: { slug, name: metadata.name || slug, imageUrl: metadata.image_url || metadata.imageUrl || "", floorPriceETH }, count, estimateQuality: estimate.estimateQuality, error }); }
