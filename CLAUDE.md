# Claude Instructions

Read `AGENTS.md` first.

Then follow:

- `.agents/agents/ORCHESTRATOR.md`
- `.agents/registry.yaml` when present
- `.agents/policies/*` when relevant

Do not treat this file as a separate source of truth. This file only adapts the shared project rules for Claude-based workflows.